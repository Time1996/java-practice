clss Solution{
	public:
		int calculateTime(string keybord, string word){
			for(int i = 0; i < keyboard.length; i++){
				for(int j = 0; j < word.length; j++){
					if(keyboard[j] == word[i]{
						sum = sum + j;//其实还有个取位置步骤的但是我不会
							}
				}
			}
		}
}
